apiVersion: v1
kind: Pod
metadata:
  name: ticketlogging-pod
  labels:
    app: ticketlogging-BE
spec:
  containers:
  - name: ticketlogging-container
    image: radiagulzan/ticketlogging:latest
    ports:
    - containerPort: 80